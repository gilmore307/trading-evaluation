# Memory

This repository records durable evaluation-system decisions in `docs/05_decision.md` and active work in `docs/04_task.md`.

Do not store secrets, generated replay artifacts, settlement reports, or runtime outputs in Git. Durable non-SQL artifacts belong under `trading-storage` contracts.
