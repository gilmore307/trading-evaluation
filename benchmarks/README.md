# Benchmark Contracts

This directory stores reviewable promotion benchmark contracts.

Accepted promotion benchmarks must use candidate-policy replay. The preferred
replay length is about five years when coverage is complete; two years is only
the minimum acceptable holdout. Contracts must not preselect final target
tickers or carry fixed target/window component panels. Candidate contracts must
pass the benchmark validator before freeze.
