# Source

`src/trading_evaluation/` owns reusable benchmark, settlement, promotion-eligibility, and promotion-readiness helpers.

Scripts may import this package. This package must not import from `scripts/`.
